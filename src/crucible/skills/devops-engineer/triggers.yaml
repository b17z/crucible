# Trigger patterns for devops-engineer skill activation.
# Consumed by core/trigger_router.py at Phase 4.
# Migrated from the v1 SKILL.md `triggers:` field as prompt_match rules.

rules:
  - type: prompt_match
    pattern: '\b(devops|infrastructure|deployment|ci|cd|docker|kubernetes|terraform|aws|gcp|azure|monitoring|observability)\b'
    note: migrated from v1 SKILL.md triggers field
