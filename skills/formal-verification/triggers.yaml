# Trigger patterns for formal-verification skill activation.
# Consumed by core/trigger_router.py at Phase 4.
# Migrated from the v1 SKILL.md `triggers:` field as prompt_match rules.

rules:
  - type: prompt_match
    pattern: '\b(formal\ verification|invariant|specification|proof|certora|halmos|symbolic)\b'
    note: migrated from v1 SKILL.md triggers field
