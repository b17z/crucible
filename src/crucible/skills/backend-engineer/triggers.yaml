# Trigger patterns for backend-engineer skill activation.
# Consumed by core/trigger_router.py at Phase 4.
# Migrated from the v1 SKILL.md `triggers:` field as prompt_match rules.

rules:
  - type: prompt_match
    pattern: '\b(backend|api|server|database|postgres|mysql|redis|queue|microservice|rest|graphql)\b'
    note: migrated from v1 SKILL.md triggers field
